# Echo Agentic Engine 重构检查清单

## ✅ 配置文件检查

### config.json
- ✅ `configSyncInterval`: 5秒 (从300秒改为5秒)
- ✅ `maxSandboxes`: 100 (从50改为100)
- ✅ `idleTimeout`: 7200秒 (从3600秒改为7200秒)
- ✅ 删除了 `healthCheckInterval` 配置项
- ✅ 删除了 `idleCheckInterval` 配置项

## ✅ Redis表结构检查

### 表1: 引擎信息表 (engine:*)

**键格式**: `engine:{engine_url}`

**包含字段**:
- ✅ `engineUrl`: 完整待命URL (http://ip:8000/trans)
- ✅ `maxSandboxes`: 功能支持多少个沙箱 (从config.json读取)
- ✅ `usedSandboxes`: 已经用了多少沙箱
- ✅ `timestamp`: 当前时间戳 (用于判断engine心跳)

**方法实现**:
- ✅ `setEngineInfo()` - 写入引擎信息
- ✅ `getEngineInfo()` - 查询单个引擎
- ✅ `getAllEngines()` - 获取所有引擎
- ✅ `deleteEngineInfo()` - 删除引擎信息

### 表2: 沙箱信息表 (sandbox:*)

**键格式**: `sandbox:{domainID}:{sandboxID}`

**包含字段**:
- ✅ `domainID`: 租户标识
- ✅ `sandboxID`: 沙箱标识
- ✅ `engineUrl`: 所属引擎的完整待命URL (http://ip:8000/trans)
- ✅ `timestamp`: 时间戳
- ✅ `lastRequestTime`: 最近一次请求处理完成时间

**方法实现**:
- ✅ `setSandboxInfo()` - 写入沙箱信息
- ✅ `getSandboxInfo()` - 查询单个沙箱
- ✅ `getSandboxesByEngine()` - 查询指定引擎的所有沙箱
- ✅ `getAllSandboxes()` - 获取所有沙箱
- ✅ `deleteSandboxInfo()` - 删除沙箱信息
- ✅ `updateSandboxLastRequest()` - 更新最后请求时间

## ✅ ConfigSyncTask流程检查

### 执行间隔
- ✅ 每5秒执行一次

### 核心功能

#### 1. 写入引擎信息表
- ✅ 获取引擎URL (http://ip:8000/trans)
- ✅ 从config.json读取maxSandboxes
- ✅ 统计当前usedSandboxes
- ✅ 记录timestamp时间戳
- ✅ 写入Redis引擎信息表

#### 2. 检测当前鲜活沙箱
- ✅ 并发健康检查所有沙箱 (get health)
- ✅ 不健康的沙箱直接杀死
- ✅ 从Redis删除不健康沙箱记录

#### 3. 更新沙箱信息表
- ✅ 根据engine_url中的IP查找本引擎在Redis上的所有沙箱记录
- ✅ 对比最新的鲜活沙箱列表
- ✅ 多退: 删除Redis中不存在的沙箱记录
- ✅ 少补: 添加新沙箱到Redis
- ✅ 更新时间戳

## ✅ 管理接口检查

### 流程 3.2.2: 列出所有信息 (原列出所有沙箱 + 查询单个沙箱)
- ✅ 合并了原有的两个流程
- ✅ 返回完整的引擎信息表
- ✅ 返回完整的沙箱信息表
- ✅ 可能返回其他分机的信息
- ✅ 数据源: 完全从Redis读取

**响应格式**:
```json
{
  "status": "success",
  "data": {
    "engines": [...],    // 所有引擎信息
    "sandboxes": [...],  // 所有沙箱信息
    "summary": {
      "totalEngines": 1,
      "totalSandboxes": 15
    }
  }
}
```

### 流程 3.2.3: 清理空闲沙箱 (新功能)
- ✅ 输入参数: `{"idleMinutes": 5}`
- ✅ 只清理本引擎负责的沙箱
- ✅ 判断依据: 心跳时间戳和最近一次请求处理完成时间相差超过指定时长
- ✅ 销毁空闲沙箱
- ✅ 从Redis删除沙箱信息

**实现细节**:
- ✅ 获取本引擎URL
- ✅ 获取本引擎所有沙箱
- ✅ 比较lastActiveAt与cutoff_time
- ✅ 销毁空闲沙箱并删除Redis记录

### 流程 3.2.4: 创建沙箱
- ✅ 创建沙箱进程
- ✅ 写入Redis沙箱信息表
- ✅ 包含domainID, sandboxID, engineUrl, timestamp, lastRequestTime

### 流程 3.2.5: 销毁沙箱
- ✅ 销毁沙箱进程
- ✅ 从Redis删除沙箱信息

## ✅ 删除的流程检查

### 流程 3.2.7: HealthCheckTask 调用链
- ✅ 文件已删除: `/src/background_tasks/health_check_task.py`
- ✅ main.py中已移除引用
- ✅ 功能已被ConfigSyncTask替代

### 流程 3.2.8: IdleCleanupTask 调用链
- ✅ 文件已删除: `/src/background_tasks/idle_cleanup_task.py`
- ✅ main.py中已移除引用
- ✅ 空闲清理权上交给echo-agent

## ✅ 架构设计验证

### 职责分离
- ✅ Engine: 负责沙箱管理、健康检查、状态同步
- ✅ echo-agent: 负责空闲清理策略决策

### 定时任务整合
- ✅ ConfigSyncTask每5秒执行
- ✅ 包含健康检查功能
- ✅ 包含Redis表同步功能

### Redis表设计
- ✅ 引擎信息表: 支持多Engine实例
- ✅ 沙箱信息表: 记录沙箱归属和状态
- ✅ 通过engineUrl实现沙箱与引擎的关联

## ⚠️ 需要注意的点

### 1. 引擎URL获取
- ✅ 已实现自动获取本机IP
- ✅ 使用socket连接8.8.8.8获取外网IP
- ✅ 失败时降级使用配置中的host

### 2. 健康检查实现
- ✅ 使用HealthChecker.checkAllHealth()
- ✅ 并发检查,不阻塞
- ✅ 不健康沙箱直接杀死,不重建

### 3. Redis同步策略
- ✅ 多退少补策略已实现
- ✅ 对比Redis和内存中的沙箱列表
- ✅ 删除过期记录,添加新记录

### 4. 时间戳处理
- ✅ 使用UTC时间
- ✅ 格式: ISO 8601 with Z suffix
- ✅ 时间比较使用datetime对象

## ✅ 文档检查

- ✅ 创建了新架构说明文档
- ✅ 更新了后台任务文档
- ✅ 创建了重构更新总结
- ✅ 包含了迁移指南

## 总结

所有要求的改造点都已正确实现:

1. ✅ config.json配置值已更新
2. ✅ ConfigSyncTask每5秒执行
3. ✅ 引擎信息表包含完整URL、时间戳、容量信息
4. ✅ 沙箱信息表包含domainID、sandboxID、engineUrl、时间戳、最后请求时间
5. ✅ 每5秒检测健康状态并杀死不健康沙箱
6. ✅ 沙箱表同步实现多退少补
7. ✅ 3.2.2接口合并并返回完整Redis表
8. ✅ 3.2.3接口改为清理空闲沙箱
9. ✅ 3.2.4和3.2.5接口更新Redis操作
10. ✅ 删除了HealthCheckTask和IdleCleanupTask

**所有功能已验证完毕，重构成功！** ✅

---

## ✅ 测试验证

详细测试报告见: [测试验证报告.md](./测试验证报告.md)

### 测试结果摘要
- ✅ 服务启动成功
- ✅ test-copy.js 测试通过 (事件流、会话创建、prompt发送全部正常)
- ✅ Redis引擎信息表正确写入
- ✅ Redis沙箱信息表正确写入和更新
- ✅ ConfigSyncTask定时执行并同步数据
- ✅ 管理接口返回正确的Redis数据
- ✅ 清理空闲沙箱接口功能正常

### 修复的问题
1. ✅ 修复 `business_layer` → `api_layer` 导入路径错误
2. ✅ 注释掉 `fetchConfig()` 调用 (配置统一从config.json读取)
3. ✅ 修复 `updateLastActive()` → `updateSandboxLastRequest()` 方法调用
4. ✅ 修复 `setSandboxMetadata()` → `setSandboxInfo()` 方法调用
5. ✅ 删除 `updateMetadata()` 调用 (状态管理移至内存)
6. ✅ 修复 `deleteSandboxMetadata()` → `deleteSandboxInfo()` 方法调用

**测试完成时间**: 2026-03-20 01:20:00 UTC
